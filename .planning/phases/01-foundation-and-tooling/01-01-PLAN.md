---
phase: 01-foundation-and-tooling
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - eslint.config.mjs
  - src/alerts/telegram.ts
  - src/scanner/ebay.ts
  - src/scanner/deal-scorer.ts
  - src/card-resolver/resolver.ts
  - src/pricing/engine.ts
  - src/scheduler/scan-scheduler.ts
  - src/watchlist/manager.ts
  - src/watchlist/storage-json.ts
  - src/types/index.ts
  - src/cli.ts
  - src/server.ts
  - src/index.ts
autonomous: true
requirements: []

must_haves:
  truths:
    - "`npm run lint` exits with code 0 and zero errors across all source files"
    - "`npm test` passes all 13 existing tests with zero warnings (no CJS deprecation)"
    - "`npm run typecheck` exits with code 0 (zero TypeScript errors)"
    - "`import { Bot } from 'grammy'` and `import eBayApi from 'ebay-api'` resolve without TypeScript errors"
  artifacts:
    - path: "eslint.config.mjs"
      provides: "ESLint 9 flat config with typescript-eslint recommended rules"
      contains: "tseslint.configs.recommended"
    - path: "package.json"
      provides: "Updated dependencies, devDependencies, and scripts"
      contains: "typecheck"
  key_links:
    - from: "eslint.config.mjs"
      to: "src/**/*.ts"
      via: "eslint src/ command"
      pattern: "tseslint"
    - from: "package.json"
      to: "tsc --noEmit"
      via: "typecheck script"
      pattern: "typecheck.*tsc --noEmit"
---

<objective>
Install and configure the complete development toolchain: ESLint with TypeScript support, Vitest upgrade, grammy and ebay-api library installation, and typecheck npm script.

Purpose: Establish a clean, warning-free development environment so all subsequent Phase 2+ module work builds on a linted, type-checked, properly-tested foundation.
Output: Working `npm run lint`, `npm test`, `npm run typecheck` commands; grammy and ebay-api importable as dependencies.
</objective>

<execution_context>
@/Users/taniatse/.claude/get-shit-done/workflows/execute-plan.md
@/Users/taniatse/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation-and-tooling/01-RESEARCH.md
@package.json
@tsconfig.json
@vitest.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dependencies, upgrade Vitest, add typecheck script</name>
  <files>package.json, package-lock.json</files>
  <action>
    1. Install ESLint toolchain as devDependencies:
       `npm install --save-dev eslint@^9 typescript-eslint @eslint/js`

    2. Upgrade Vitest from 1.x to 4.x:
       `npm install --save-dev vitest@^4`

    3. Install grammy and ebay-api as runtime dependencies (NOT devDependencies):
       `npm install grammy ebay-api`

    4. Add `typecheck` script to package.json scripts:
       `"typecheck": "tsc --noEmit"`

    5. Verify existing tests still pass after all installs:
       `npm test`
       Expected: 13 tests pass, 2 test files, zero CJS deprecation warning.

    6. Verify TypeScript still compiles cleanly:
       `npm run typecheck`
       Expected: exit code 0, zero errors.

    IMPORTANT: Do NOT modify any source files in this task. Do NOT import grammy or ebay-api into existing source code. Only install packages and update scripts.

    If Vitest 4 causes test failures, check for deprecated config options in vitest.config.ts. The current config uses only `test.include` which is safe across versions.
  </action>
  <verify>
    - `npm test` passes all 13 tests with zero warnings
    - `npm run typecheck` exits with code 0
    - `node -e "require.resolve('grammy')"` exits without error
    - `node -e "require.resolve('ebay-api')"` exits without error
    - `grep '"typecheck"' package.json` shows the new script
  </verify>
  <done>
    Vitest 4.x runs 13 tests with no CJS deprecation warning. grammy and ebay-api are installed in dependencies (not devDependencies). `npm run typecheck` script exists and passes. No source files modified.
  </done>
</task>

<task type="auto">
  <name>Task 2: Configure ESLint and fix all lint errors</name>
  <files>eslint.config.mjs, src/alerts/telegram.ts, src/scanner/ebay.ts, src/scanner/deal-scorer.ts, src/card-resolver/resolver.ts, src/pricing/engine.ts, src/scheduler/scan-scheduler.ts, src/watchlist/manager.ts, src/watchlist/storage-json.ts, src/types/index.ts, src/cli.ts, src/server.ts, src/index.ts</files>
  <action>
    1. Create `eslint.config.mjs` in project root with ESLint 9 flat config:
       ```javascript
       // @ts-check
       import eslint from '@eslint/js';
       import { defineConfig } from 'eslint/config';
       import tseslint from 'typescript-eslint';

       export default defineConfig(
         eslint.configs.recommended,
         tseslint.configs.recommended,
         {
           ignores: ['dist/**', 'node_modules/**'],
         }
       );
       ```

       IMPORTANT: Use `.mjs` extension (not `.js`) because package.json does NOT have `"type": "module"`. Using `.js` would cause a CJS/ESM import error.

    2. Run `npm run lint` to see all errors. Expected common errors from typescript-eslint recommended:
       - `@typescript-eslint/no-explicit-any` — replace `any` with proper types or use `unknown` where type is genuinely unknown
       - `@typescript-eslint/no-unused-vars` — remove unused variables or prefix with underscore `_`
       - `no-undef` — may fire on Node.js globals; if so, add `globals` configuration for node environment
       - `@typescript-eslint/no-require-imports` — replace any `require()` with `import`

    3. Fix ALL lint errors in source files. Approach:
       - First try `npm run lint -- --fix` for auto-fixable issues
       - Then manually fix remaining errors
       - For `no-explicit-any`: use the actual type if knowable, `unknown` if not, or add an eslint-disable comment ONLY as last resort with a justification comment
       - For unused variables in function parameters (e.g., error handlers): prefix with underscore `_err`
       - Do NOT change runtime behavior — lint fixes should be type annotations, unused var cleanup, and similar non-behavioral changes

    4. If Node.js globals (`process`, `console`, `Buffer`, `setTimeout`, etc.) trigger `no-undef` errors, update eslint.config.mjs to include node globals. Add `import globals from 'globals';` and include `{ languageOptions: { globals: globals.node } }` in the config array. Install `globals` package if needed: `npm install --save-dev globals`.

    5. Run `npm run lint` — must exit with zero errors.

    6. Run `npm test` — must still pass all 13 tests (lint fixes should not break tests).

    7. Run `npm run typecheck` — must still pass (lint fixes should not break types).

    SCOPE DISCIPLINE: Only fix lint errors. Do NOT refactor modules, rename files, change architecture, or integrate grammy/ebay-api into source code. That is Phase 2 work.
  </action>
  <verify>
    - `npm run lint` exits with code 0 and zero errors
    - `npm test` passes all 13 tests
    - `npm run typecheck` exits with code 0
    - `cat eslint.config.mjs` shows flat config with tseslint.configs.recommended
  </verify>
  <done>
    ESLint 9 with typescript-eslint recommended rules is configured. All source files in src/ pass linting with zero errors. Existing tests and type checks still pass. No runtime behavior changes.
  </done>
</task>

</tasks>

<verification>
Run all four success criteria commands in sequence:

1. `npm run lint` — exits 0, zero errors
2. `npm test` — 13 tests pass, no CJS deprecation warning
3. `npm run typecheck` — exits 0, zero TypeScript errors
4. Verify library importability:
   ```bash
   npx tsx -e "import { Bot } from 'grammy'; import eBayApi from 'ebay-api'; console.log('OK');"
   ```
   Expected output: `OK`
</verification>

<success_criteria>
1. `npm run lint` passes with zero errors across all source files
2. `npm test` runs with Vitest and all existing tests pass without warnings
3. `grammy` and `ebay-api` are installed and importable
4. `npm run typecheck` exits with zero TypeScript errors
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation-and-tooling/01-01-SUMMARY.md`
</output>
