---
phase: 07-phase2-completion
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/pricing/engine.ts
  - src/types/index.ts
  - src/card-resolver/resolver.ts
  - package.json
  - package-lock.json
autonomous: true
requirements: [SCAN-03, SCAN-04, SCAN-05, WATCH-04]
must_haves:
  truths:
    - "npm run lint exits with zero errors"
    - "npm run typecheck exits with zero errors"
    - "npm test passes (all 71+ tests pass, none reference rawConfidence)"
    - "ebay-api is not listed in package.json dependencies"
    - "CardCandidate has no rawConfidence field in src/types/index.ts"
    - "ConditionPrice interface does not exist in src/pricing/engine.ts"
  artifacts:
    - path: "src/pricing/engine.ts"
      provides: "PriceEngine — ConditionPrice interface removed"
    - path: "src/types/index.ts"
      provides: "CardCandidate type — rawConfidence field removed"
    - path: "src/card-resolver/resolver.ts"
      provides: "buildV2Candidates — rawConfidence assignment removed"
    - path: "package.json"
      provides: "Dependencies — ebay-api absent"
  key_links:
    - from: "src/card-resolver/resolver.ts"
      to: "src/types/index.ts"
      via: "CardCandidate type usage"
      pattern: "rawConfidence"
---

<objective>
Remove three dead-code/unused items identified in the v1.0 audit: the `ConditionPrice` unused interface that causes `npm run lint` to fail, the `rawConfidence` field on `CardCandidate` that is set but never read, and the `ebay-api` npm package that is installed but never imported.

Purpose: Restore the Phase 1 "zero lint errors" guarantee and eliminate misleading dead code.
Output: Clean lint pass, clean typecheck, no unused dependencies.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-phase2-completion/07-CONTEXT.md
@src/pricing/engine.ts
@src/types/index.ts
@src/card-resolver/resolver.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove ConditionPrice interface and rawConfidence dead code</name>
  <files>src/pricing/engine.ts, src/types/index.ts, src/card-resolver/resolver.ts</files>
  <action>
    1. In `src/pricing/engine.ts` lines 37-42: Delete the entire `ConditionPrice` interface block (it is defined but never used anywhere — the lint error is `no-unused-vars` on this interface). Do not touch any other interfaces or logic.

    2. In `src/types/index.ts` lines 18-20: Remove the `rawConfidence` field from `CardCandidate`:
       - Remove: `rawConfidence?: number; // unmodified API matchScore, used by two-tier gate`
       - The comment says "used by two-tier gate" but the actual gate code in resolver.ts uses the adjusted `confidence`, not `rawConfidence`. The field is dead.

    3. In `src/card-resolver/resolver.ts` in `buildV2Candidates()`: Remove the line that sets `rawConfidence: matchConfidence` when constructing the `CardCandidate` object. It is the only assignment to this field. The `matchConfidence` variable itself is still used in `baseConfidence` computation — only remove the `rawConfidence` property from the pushed object, not the `matchConfidence` variable.

    After changes, run: `npm run lint` and `npm run typecheck` to confirm zero errors.
  </action>
  <verify>
    Run from gacha-agent-core/:
    - `npm run lint` — must exit 0 with no errors
    - `npm run typecheck` — must exit 0 with no errors
    - `npm test` — all tests must pass (rawConfidence was never tested — no test should break)
    - `grep -r "rawConfidence" src/` — must return no results
    - `grep -r "ConditionPrice" src/` — must return no results
  </verify>
  <done>
    lint: 0 errors, typecheck: 0 errors, tests: all pass, no rawConfidence or ConditionPrice in src/
  </done>
</task>

<task type="auto">
  <name>Task 2: Uninstall ebay-api npm package</name>
  <files>package.json, package-lock.json</files>
  <action>
    Run: `npm uninstall ebay-api`

    This removes `ebay-api` from `package.json` dependencies and updates `package-lock.json`. The package was installed in Phase 1 but `EbayScanner` uses native `fetch` with manual OAuth — `ebay-api` is never imported in any source file.

    Confirm by running: `grep -r "ebay-api" src/` — must return no results (proving it was never imported).
    Then: `cat package.json` — confirm `ebay-api` is absent from dependencies.

    Run `npm test` after uninstall to confirm no runtime dependency on the package.
  </action>
  <verify>
    - `grep "ebay-api" package.json` — must return nothing
    - `npm test` — all tests pass
  </verify>
  <done>
    ebay-api absent from package.json, all tests pass after removal
  </done>
</task>

</tasks>

<verification>
After both tasks:
1. `npm run lint` — zero errors
2. `npm run typecheck` — zero errors
3. `npm test` — all 71+ tests pass
4. `grep -r "rawConfidence\|ConditionPrice" src/` — no matches
5. `grep "ebay-api" package.json` — no match
</verification>

<success_criteria>
- npm run lint exits 0 with zero errors (was failing with 1 error on ConditionPrice)
- npm run typecheck exits 0
- npm test passes all tests
- No rawConfidence field in CardCandidate or resolver.ts
- No ConditionPrice interface in engine.ts
- ebay-api absent from package.json
</success_criteria>

<output>
After completion, create `.planning/phases/07-phase2-completion/07-01-SUMMARY.md`
</output>
